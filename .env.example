# Copia este archivo como ".env.local" y coloca ahí tus valores reales.
# ".env.local" nunca se sube a git (ver .gitignore).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# NUNCA agregues aquí la SUPABASE_SERVICE_ROLE_KEY.
# Esa clave tiene privilegios totales y jamás debe existir en código de frontend.
